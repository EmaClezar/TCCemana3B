<!DOCTYPE html>
<html lang="pt-BR">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Editar Fornecedor</title>
    <link rel="stylesheet" href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0-alpha3/dist/css/bootstrap.min.css">
    <style>
        .form-container {
            max-width: 900px;
            margin: 50px auto;
            padding: 40px;
            background-color: #f8f9fa;
            border-radius: 10px;
            box-shadow: 0 10px 30px rgba(0, 0, 0, 0.1);
        }
        .form-group {
            margin-bottom: 20px;
        }
        .form-group label {
            font-weight: bold;
        }
        button[type="submit"] {
            background-color: #0259b7;
            color: white;
            border: none;
            padding: 12px 20px;
            font-size: 1.2rem;
            font-weight: bold;
            border-radius: 8px;
            width: 100%;
        }
        .btn-view {
            background-color: #28a745;
            color: white;
            border: none;
            padding: 10px 20px;
            font-size: 1.1rem;
            border-radius: 8px;
            margin-top: 20px;
            width: 100%;
        }
        .btn-view:hover {
            background-color: #218838;
        }
    </style>
</head>
<body>
    <div class="form-container">
        <h1>Editar Fornecedor</h1>
        <form action="/fornecedores/<%= fornecedor.id %>?_method=PUT" method="POST">
            <!-- Campo para o Nome -->
            <div class="form-group">
                <label for="nome">Nome:</label>
                <input type="text" class="form-control" name="nome" value="<%= fornecedor.nome %>" required>
            </div>

            <!-- Campo para o CPF/CNPJ -->
            <div class="form-group">
                <label for="cpf_cnpj">CPF/CNPJ:</label>
                <input type="text" class="form-control" name="cpf_cnpj" value="<%= fornecedor.cpf_cnpj %>" required>
            </div>

            <!-- Campo para o Email -->
            <div class="form-group">
                <label for="email">Email:</label>
                <input type="email" class="form-control" name="email" value="<%= fornecedor.email %>" required>
            </div>

            <!-- Campo para o Telefone -->
            <div class="form-group">
                <label for="telefone">Telefone:</label>
                <input type="text" class="form-control" name="telefone" value="<%= fornecedor.telefone %>" required>
            </div>

            <!-- Campo para o Endereço -->
            <div class="form-group">
                <label for="endereco">Endereço:</label>
                <input type="text" class="form-control" name="endereco" value="<%= fornecedor.endereco %>" required>
            </div>

            <!-- Botão de envio -->
            <button type="submit">Salvar Alterações</button>
        </form>

        <!-- Botão de Visualização do Fornecedor -->
        <a href="/fornecedores/<%= fornecedor.id %>" class="btn btn-view">Ver Fornecedor</a>
    </div>
</body>
</html>
