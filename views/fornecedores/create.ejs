<!DOCTYPE html>
<html lang="pt-BR">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Criar Fornecedor</title>
    <link rel="stylesheet" href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0-alpha3/dist/css/bootstrap.min.css">
    <style>
        /* Estilo da Navbar */
        .navbar {
            background-color: #0259b7; /* Azul Escuro */
            padding: 1rem 2rem;
            border-bottom: 3px solid #004085; /* Sombra de destaque */
        }

        .navbar-brand {
            color: white;
            font-size: 1.8rem;
            font-weight: bold;
            display: flex;
            align-items: center;
        }

        .navbar-brand img {
            height: 50px;
            margin-right: 15px;
        }

        .navbar-nav .nav-link {
            color: white;
            font-size: 1.1rem;
            margin-left: 20px;
            transition: color 0.3s;
        }

        .navbar-nav .nav-link:hover {
            color: #cce5ff; /* Azul claro ao passar o mouse */
        }

        .navbar-nav .dropdown-menu {
            background-color: #0056b3; /* Azul mais escuro */
            border: none;
        }

        .dropdown-item {
            color: white;
            padding: 10px 20px;
        }

        .dropdown-item:hover {
            background-color: #004085;
            color: #cce5ff;
        }

        /* Estilo da caixinha de formulário */
        .form-container {
            max-width: 900px;
            margin: 50px auto;
            background-color: #fff;
            padding: 30px;
            border-radius: 8px;
            box-shadow: 0 5px 15px rgba(0, 0, 0, 0.1);
            background-color: #f8f9fa;
            border-top: 5px solid #0259b7;
        }

        /* Título */
        .form-container h1 {
            color: #0259b7;
            text-align: center;
            margin-bottom: 30px;
            font-size: 2.5rem;
            font-weight: bold;
        }

        /* Estilo dos campos do formulário */
        .form-group label {
            font-size: 1.1rem;
            color: #495057;
            font-weight: 600;
        }

        .form-control {
            border-radius: 8px;
            border: 1px solid #ced4da;
            padding: 12px 15px;
            font-size: 1.1rem;
            transition: border 0.3s, box-shadow 0.3s;
        }

        .form-control:focus {
            border-color: #0259b7;
            box-shadow: 0 0 5px rgba(0, 123, 255, 0.5);
        }

        /* Botão de envio */
        button[type="submit"] {
            background-color: #0259b7;
            color: #fff;
            border: none;
            padding: 12px 20px;
            font-size: 1.2rem;
            font-weight: bold;
            border-radius: 8px;
            width: 100%;
            transition: background-color 0.3s, transform 0.3s;
        }

        button[type="submit"]:hover {
            background-color: #004085;
            transform: translateY(-2px);
        }

        button[type="submit"]:active {
            transform: translateY(2px);
        }

        /* Ajuste no espaçamento dos campos do formulário */
        .form-group {
            margin-bottom: 25px;
        }
    </style>
</head>
<body>
    <div class="form-container">
        <h1>Criar Novo Fornecedor</h1>
        <form action="/fornecedores" method="POST">

            <!-- Campo para Nome -->
            <div class="form-group">
                <label for="nome">Nome:</label>
                <input type="text" class="form-control" id="nome" name="nome" required>
            </div>

            <!-- Campo para CPF/CNPJ -->
            <div class="form-group">
                <label for="cpf_cnpj">CPF/CNPJ:</label>
                <input type="text" class="form-control" id="cpf_cnpj" name="cpf_cnpj" required>
            </div>

            <!-- Campo para Email -->
            <div class="form-group">
                <label for="email">Email:</label>
                <input type="email" class="form-control" id="email" name="email" required>
            </div>

            <!-- Campo para Telefone -->
            <div class="form-group">
                <label for="telefone">Telefone:</label>
                <input type="text" class="form-control" id="telefone" name="telefone" required>
            </div>

            <!-- Campo para Endereço -->
            <div class="form-group">
                <label for="endereco">Endereço:</label>
                <input type="text" class="form-control" id="endereco" name="endereco" required>
            </div>

            <!-- Botão de envio -->
            <button type="submit">Criar Fornecedor</button>

            
        </form>
    </div>
</body>
</html>
